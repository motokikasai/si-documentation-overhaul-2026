# OS junk
.DS_Store

# Redundant zip snapshots (the folders they mirror are tracked)
*.zip

# Python scratch
__pycache__/
*.pyc

# live DB dumps — never commit (hundreds of MB)
db/

# Day-3 photo pipeline scratch (regenerable: dump index, fetched frames)
sessions/*/incoming/.cache/
sessions/*/incoming/framegrabs/
