/* Entry for the tree-shaken three build. Lists exactly the symbols
 * si-hero-scene.js uses (keep in sync — see build/README.md). */
export {
  AdditiveBlending, AmbientLight, BackSide, BufferGeometry, Clock, Color,
  DirectionalLight, Float32BufferAttribute, Group, Line, LineBasicMaterial,
  LinearSRGBColorSpace, MathUtils, Mesh, MeshStandardMaterial, PerspectiveCamera,
  Points, SRGBColorSpace, Scene, ShaderMaterial, SphereGeometry, TextureLoader,
  Vector3, WebGLRenderer,
} from "three";
